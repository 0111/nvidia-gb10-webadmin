# 任务执行跟踪

## 项目信息
- **项目名称**：NVIDIA GB10 Manager - 本地大模型管理工具
- **目标平台**：NVIDIA DGX Spark / GB10 Grace Blackwell (ARM64)
- **服务器**：DgxSpark (192.168.199.4)，项目目录 `/home/spark/nvidia-gb10-manager`
- **开发方式**：claude + SSH 直接登录 DgxSpark 服务器开发（编辑/构建/运行/测试/git 提交均在服务器本机完成，不再有独立 Windows 开发机）
- **开始日期**：2026-06-26
- **最后更新**：2026-06-27

## 重要说明
服务器上此前存在一份独立推进到 v0.3.0 的旧项目代码，因与本机版本脱节，已改名备份为
`/home/spark/nvidia-gb10-manager.bak-20260627`（未删除，仅供参考），不再使用。
现已改为直接在 DgxSpark 服务器本机用 claude + SSH 开发，服务器仓库即唯一权威版本；
早期那套「Windows 本机改码 → `tools/remote.py`（paramiko 非交互 SSH）同步」的流程已不再使用（`tools/remote.py` 作为遗留工具保留）。

## 版本发布记录

| 版本 | 日期 | 内容 |
| --- | --- | --- |
| v2.3.16 | 2026-07-02 | 推荐开源大模型.md 新增「最不推荐清单(避坑)」章节:按严重度分三档排序——🔴第一档基本别用(GPTOSS-120B MXFP4 GGUF 无法加载/valid=False、*-Aggressive-NVFP4 数值脆满屏!、GLM-4.7-Flash-Aggressive GGUF)、🟠第二档纯浪费显存(未量化 bf16 大模型:Qwen3.6-35B 67G/DeepSeek-R1-32B 61G/Qwen3-VL-30B 58G/GLM-4.7-Flash 58G,均有 NVFP4/FP8 同款应替换)、🟡第三档老/受限(QwQ-32B 上下文仅40960+无工具、Qwen3.6-27B 稠密大而弱、老GGUF单文件)。含各自更优替代与一句话总结 |
| v2.3.15 | 2026-07-02 | 新增 docs/推荐开源大模型.md:基于本机实际扫描(29通用+5嵌入)按 AI 使用场景做开源大模型推荐清单。结合 GB10 128GB 统一内存/带宽瓶颈特点给选型原则(优先 NVFP4/FP8、MoE 适配、共存留余量);按场景分组(通用对话/编码开发/多模态图文分析/超长上下文1M/推理/最强大参数/安全渗透/RAG嵌入重排)各给首选+备选+显存+加载要点;附场景速查表与「不建议/需注意」(GPTOSS-120B MXFP4 GGUF 无法加载、aggressive-NVFP4 数值脆、未量化大模型优先用量化版、dtype 勿设 float16) |
| v2.3.14 | 2026-07-02 | max_model_len 与 max_num_batched_tokens 下拉改为以「k」显示。前端新增 optionLabel(key,opt),仅对这两个上下文档位把整千位 token 数换算成 k(65536→64k、524288→512k、786432→768k、1048576→1024k);下拉 value 仍为原始 token 数,提交给后端不变。其它下拉(kv_cache_dtype/dtype/tokenizer_mode/tool_call_parser/convert/runner/max_num_seqs)保持原样(如 max_num_seqs=2 仍显示 2、fp8 仍显示 fp8)。基础参数与高级参数两处 <option> 均已应用。node 校验换算正确 + 前端 build 通过 |
| v2.3.13 | 2026-07-02 | 上下文档位扩展到 512k/768k/1024k,支持 1M 上下文模型(如 Qwythos-9B-Claude-Mythos-5-1M,max_position_embeddings=1048576)。问题:该模型 max_num_batched_tokens 与 max_model_len 下拉最高只到 256k。根因:param_advisor 的 CONTEXT_LEN_OPTIONS 档位表封顶 262144。修复:档位表新增 524288/786432/1048576(512k/768k/1024k)。说明:这两个参数本来就是下拉(<select>,带 options=按模型 max_position_embeddings 裁剪后的档位),此次只是把档位上限提到 1024k;各模型仍按其 max_position_embeddings 自动封顶——1M 模型显示到 1024k,256k 模型仍只到 256k。真机验证:/params 对 1M 模型两参数均出现 512k/768k/1024k,262144 模型不误增 |
| v2.3.12 | 2026-07-02 | 模型输出满屏 '!' 排障 + 新增 enforce_eager 稳定性选项。现象:Qwen3.6-35B-A3B-Uncensored NVFP4 调接口输出连续 '!' 且请求卡住超时。诊断:'!' 是 logits 出现 NaN(采样退化到 token id 0=Qwen 分词器的 '!');vllm /metrics 显示 2 个请求 running 但 kv_cache 仅 0.55% 不增长=引擎已 wedged 卡死。根因为该 aggressive NVFP4 合并量化偶发数值异常导致引擎卡死,非网关/参数问题(同栈其它模型均正常)。处置:重载模型清除卡死状态后,真机验证短/数学/~2k token 长输入全部正常生成(不再 '!')。新增可选参数 enforce_eager(param_advisor 暴露 + docker_helper bool_flag_map → --enforce-eager,vllm 0.22.1 支持):关闭 CUDA graph 用 eager 执行,规避个别量化模型 CUDA graph 重放数值异常;默认不勾选,高级参数可开启。真机:/params 暴露 enforce_eager、compose 正确 emit/不emit、模型健康 200 输出正常 |
| v2.3.11 | 2026-07-02 | 运行观测「模型级 tok/s」四图科学排版与正确显示。问题:最近加载的模型(通用35B仅~60min数据)在整天窗口被挤到最右约5%、左侧一大片空白;4图平铺一行又矮又挤;嵌入模型 Decode 恒为0 的图无意义。修复:①tpsSeries 按每个模型『首次产生数据』下标裁剪到其活跃区间(真机 general 由 7488 点→365 点/~60min 铺满整宽,embedding 保持全跨度),同模型 prefill/decode 共用起点横轴对齐;②改为按模型分组卡片(模型名只出现一次,prefill+decode 并排,minmax440px 自适应),Sparkline 加高到 96px;③嵌入模型不再绘制空的 Decode 图,改文字说明『只做前向编码、无自回归 decode』;④说明补充 prefill 天然脉冲状、横轴按加载后跨度绘制。前端 build 通过 |
| v2.3.10 | 2026-07-02 | 修复 OpenWebUI chat 流式报 'TransferEncodingError: Not enough data to satisfy transfer length header'。根因:web.log 显示网关 event_stream 打开到 vllm 的流时瞬时抛 httpx.ReadError/RemoteProtocolError('Server disconnected without sending a response'),原流式路径无重试/无异常处理→生成器抛异常→chunked SSE 未发送终止块→客户端(aiohttp)报 payload 不完整。直连 :8001 流式与网关短请求均正常(非必现,属瞬时上游断连)。修复:网关流式打开对 ConnectError/ReadError/RemoteProtocolError/ConnectTimeout 重试 3 次(仅在尚未向客户端发出任何字节时重试,避免污染 SSE),重试耗尽或已开始传输后失败则以 {error}+[DONE] 事件干净终止流(客户端收到错误消息而非截断体)。真机 happy path 82 chunk+[DONE]+0 error 正常 |
| v2.3.9 | 2026-07-02 | 修复上传图片分析报 'No space left on device'。现象:OpenWebUI 上传图片给多模态模型分析即报该错,纯文本 chat 正常;宿主磁盘 1.9T 空闲(非真磁盘满)。根因:docker 容器默认 /dev/shm 仅 64MB,vllm/torch 处理图片多模态张量写共享内存瞬间耗尽→No space left on device;而 render_compose 的 COMPOSE_TEMPLATE 从未设置 shm_size。修复:模板新增 shm_size:'16gb'(tmpfs 按需占用、非预留,128GB GB10 上安全)。真机验证:重建后容器 /dev/shm=16G(原 64M)。注:需重载既有模型使新 compose 生效 |
| v2.3.8 | 2026-07-02 | vllm serve 改用位置参数传模型,消除 --model 废弃告警。当前容器 vllm 0.22.1,真机启动日志告警 '--model option will be removed in a future version, provide the model as a positional argument'。render_compose 由 'vllm serve --model <path>' 改为 'vllm serve <path>'(位置参数 model_tag)。已核对全部 emit 旗标:真机启动日志中仅 --model 触发废弃告警,其余(gpu-memory-utilization/max-model-len/max-num-batched-tokens/kv-cache-dtype/dtype/quantization/tokenizer-mode/tool-call-parser/convert/runner/enable-*/trust-remote-code)均正常无告警;transformers 的 Qwen2VLImageProcessorFast/use_fast 告警属 vllm 内部,非本项目可控。真机重载嵌入模型验证:日志 model_tag 位置参数、无 --model 告警、加载正常 |
| v2.3.7 | 2026-07-02 | 校准 gpu_memory_utilization 上限提醒:GB10 上 pynvml GPU 显存 Not Supported,psutil free 比 vllm 启动时检查的 CUDA free 高约 3-4GiB(CUDA context/driver 开销),导致原 suggested_max=free/total 偏乐观(照提示填 0.2 仍 OOM by a hair)。改为预留 4GiB 缓冲 suggested_max=floor((free-4)/total),保守但可靠可加载;前端文案标注'保守估算,已预留显存开销,实际以启动时显存为准'。真机 free27.24/total121.69→0.19(校准前0.21);并清理复现测试残留的失败嵌入容器 |
| v2.3.6 | 2026-07-02 | 修复加载失败后页面误显示'已加载/卸载' + gpu_memory_utilization 可用上限提醒。①bug:/api/overview 与 /api/models 的 load_status 仅取内存 registry(只记录已发起加载),容器崩溃后仍显示已加载;新增 state.real_load_status() 按真实 docker State.Status 判 running/failed,overview 拆出 *_models_failed、models 列表崩溃→error,前端总览失败模型显示红色「加载失败」+「清理」按钮而非「已加载/卸载」,模型页状态列中文化。真机复现嵌入模型 OOM 崩溃→overview embedding_models_failed 命中、load_status=error。②增强:/params 返回 gpu_memory_hint(free/total/suggested_max≈free/总),通用/嵌入配置页 gpu_memory_utilization 下方提醒当前可设最大值(仅提醒非强制)。真机 free26.72/total121.68→建议≤0.21 |
| v2.3.5 | 2026-07-01 | 本地模型扫描/审计补充辅助信息并在通用/嵌入模型页详细展示 + 修复 OpenWebUI 图片分析工具报错。①scanner/audit/API 新增:model_type(哪种模型)、is_moe+num_experts+num_experts_per_tok(MoE 专家总/激活)、param_count(参数量,量化为打包计数近似)、hidden_size/num_hidden_layers、multimodal/modalities(是否支持图片/视频)。②GeneralModelView(通用+嵌入两页共用)选中模型后新增「模型分析」卡详细展示上述字段。③修复 OpenWebUI 上传图片报错:tool-capable 模型默认启用 enable_auto_tool_choice(嵌入/pooling 除外),且 tool_call_parser 改为按 chat_template 格式判定(含<function=→qwen3_xml/Coder,含<tool_call>→hermes/Qwen3-VL),解决 qwen3_xml 误解析 Qwen3-VL 输出致 tool_call name=None、回传历史 400 的问题。真机验证:33 模型分类正确;Qwen3-VL 重载 parser=hermes,工具调用 name 有效+多轮回传 200+图片分析 200 |
| v2.3.4 | 2026-07-01 | 多模态能力检测 + Qwen3-VL 图片分析排障。①model_scanner 新增 multimodal/modalities 检测(image=vision_config/图像处理器;video=video_preprocessor/video_config;audio=audio_config——按结构化信号而非裸 token_id,避免 Gemma 等预留 token 误报视频/音频);audit 增「是否支持多模态/图片分析/工具功能」行(含图片需足够 max_model_len/batched_tokens 提示);ModelInfoOut+通用模型页新增「多模态」列。真机重扫 33 模型分类正确。②Qwen3-VL 图片分析排障:实测模型+网关+OpenAI/Claude 格式对 336~2688px 图片全部 200 且正确识别(2688px≈9216tok 超 batched 8192 仍成功,非 token 限制)——真正病因是 gpu_memory_utilization=0.9 与嵌入模型共存时启动 OOM(free91<desired109GiB)容器崩溃致 API 报错;降 0.6 共存正常 |
| v2.3.3 | 2026-07-01 | 运行观测 tok/s 图峰值重复显示修复:模型级图表原来既在 .chart-value 头部渲染「峰值 tpsPeak()」又给 Sparkline 传 show-peak(组件内再渲染同值峰值)导致峰值显示两次;移除头部冗余 span 及死代码 tpsPeak()/.chart-peak CSS,保留组件 show-peak(v2.0.0 专为此加),与系统级图表布局对齐。npm build 通过,ObservabilityView bundle 10.90→10.63kB |
| v2.3.2 | 2026-07-01 | 运行观测 tok/s 趋势图非连续修复:Sparkline 遇 null/NaN 由断成多段改为跨 gap 桥接(跳过缺失点但不抬笔,下一有效点用 L 连回,x 仍用原始索引保证水平跨度正确)。根因是嵌入模型 prefill 序列大多为 idle-0,其'缺失'来自模型未加载/未采样的 tick(全天缺席 1018/8728),此前分段渲染(v2.0.0 防 NaN 整线消失的副作用)导致视觉非连续。node 模拟代表性带 gap 序列由 3 段断线→1 条连续线;前端 build 通过 |
| v2.3.1 | 2026-06-30 | 复用 vllm 0.22.1 原生 Claude/OpenAI API 去同质化 + 补丁镜像缺失自愈。①debug_router api_format=claude 改直连原生 /v1/messages 返回真实响应(删除 Claude→OpenAI 翻译与伪造信封),新增 web/anthropic_compat.py 把 system 角色消息收敛到顶层 system 的逻辑抽成 gateway/debug 共享模块;网关因原生 /v1/messages 对 system 角色 400、对 x-api-key 401 而保留(确认非同质)。②新增 core.docker_helper.ensure_vllm_image()/image_exists():加载与 cli start 前检查 config.vllm_image,缺失且=补丁tag则自动 docker build deploy/vllm-patch.Dockerfile(幂等),缺失且非补丁tag则返回明确错误(不再让 docker compose 去 pull 本地tag报晦涩错),已存在则no-op;并入加载失败检测。真机验证:claude调试 url=/v1/messages、type=message、无_source;openai回归200;网关 system-in-messages 200;ensure 三分支(存在/非补丁缺失/build命令)全过;web health 200 |
| v2.3.0 | 2026-06-30 | #1 模型加载失败检测:组件状态对已登记但容器exited/dead/restarting的槽位改报failed+原因(此前硬编码running,导致死掉的模型仍显示已加载);load端点docker compose up后短轮询(3x2s)捕获秒退崩溃,立即返回accepted=false+日志错误摘要且不误登记;前端Overview把failed/exited/dead渲染为红点并显示detail。#4 模型完整性审计增强:model_scanner新增tokenizer存在/完整性校验(safetensors缺词表文件判无效,GGUF不误报)+model_type/tokenizer字段;新增core/model_audit.py与GET /api/models/{name}/audit,consolidate config/tokenizer/shard完整性+架构/类型/量化/上下文/权重大小/vLLM支持/推荐启动参数/静态校验结论/错误摘要。真机:全量33模型重扫仅1个真无效(GPTOSS-120B MXFP4),审计端点字段正确 |
| v2.2.2 | 2026-06-30 | 修复升级26.06后/v1/*推理接口全部500的根因(并非参数问题):基础镜像fastapi0.137.1新增无.path的_IncludedRouter路由类,与prometheus_fastapi_instrumentator8.0.0中间件不兼容(读route.path抛AttributeError),/metrics在excluded_handlers里故200而chat/embeddings必500。新增deploy/vllm-patch.Dockerfile(sed改getattr容错)构建gb10-vllm:26.06-py3-patched,config.vllm_image默认指向它,bootstrap第[5/5]步幂等构建。真机验证:补丁镜像上embedding(2048维)/chat(30B真实生成)/网关8000全部200 |
| v2.2.1 | 2026-06-30 | 修复 SearXNG 容器日志错误:① ahmia/torch 暗网(onion)引擎无 Tor 代理时加载即报 set engine to inactive——用 use_default_settings.engines.remove 彻底移除(disabled 不够,被禁用引擎仍会加载);② startpage 被数据中心 IP 返回验证码页致 JSONDecodeError——disabled 禁用(与 brave 同;不能 remove 否则破坏共享 network 导致 KeyError 崩溃);③ searxng_client 请求补 X-Forwarded-For/X-Real-IP=127.0.0.1 消除 botdetection 的 'X-Forwarded-For nor X-Real-IP header is set' ERROR。真机验证:容器启动零 ERROR/WARNING,API 搜索返回 17 条结果 |
| v2.2.0 | 2026-06-30 | 项目结构重新规划+清理:7个研发文档归档到 docs/(README/上下文交接/release.sh 引用同步更新);删除过期文件(tools/remote.py 遗留paramiko远程工具+tools/gen_favicon.py 一次性脚本+data/merged-gguf+26.05备份);修复 Project_Task.md 失效链接(改指 docs/技术方案+研发方案);新增 tools/make_release.sh 基于 git archive 打发布包(release/,只含git跟踪文件,自动排除运行态/密钥/node_modules/dist);release.sh 版本表路径改 docs/Task_Tracking.md;上下文交接清理 ollama/remote.py 残留描述 |
| v2.1.1 | 2026-06-30 | 新增根目录 cli.sh 包装器(自动用 .venv 解释器执行 cli.main,免手动 source 激活,任意目录可调用);完成 vllm 26.06/cuda-13.3 重新部署:删除旧镜像版的 data/compose 渲染文件(备份至 data/compose-old-26.05.bak 并加入 .gitignore),起基础服务(SearXNG+Web后端+前端),真机验证 /api/settings 返回 26.06-py3 + cuda-13.3、web health 200、前端 4173 200;旧镜像 26.05.post1 按用户选择保留 |
| v2.1.0 | 2026-06-30 | vllm 容器升级 26.05.post1-py3(0.21)→26.06-py3(0.22.1)、cuda-compat-13-2→13-3(宿主路径/usr/local/cuda-13.3/compat)；全量代码+文档同步该版本；param_advisor 按 0.22.1 真机参数更新(tool-call-parser+apertus / quantization+auto_gptq / kv-cache-dtype+nvfp4(Blackwell) / tokenizer-mode+hf)；真机验证新镜像 vllm 0.22.1+CUDA13.3、cuda-compat-13-3 注入后容器内 torch.cuda 可见 GB10、/metrics 指标名与 GGUF 支持集合不变、--task 仍移除 |
| v2.0.0 | 2026-06-30 | 高级设置SearXNG服务地址改显示局域网可达地址(hostname+port);移动端/iPad响应式排版(宽表横滚/导航横滑/内边距收窄,Sparkline宽度自适应);运行观测模型级tok/s显示峰值(最大值)并修复Sparkline含NaN时整线消失的bug;文档全面更新(checklist阶段十四+上下文交接+操作手册+研发方案+README) |
| v1.9.0 | 2026-06-29 | 运行观测模型级tok/s改为持久化趋势图(与系统级同源同窗口):tok/s采样移入MetricsCollector常驻tick作单一差分源(LATEST_TPS),持久化进metrics_history的model_tps字段并经/api/metrics/history返回(补schema/router);runtime_stats改读LATEST_TPS不再自行差分(消除双重差分);前端ObservabilityView改用history派生tpsSeries+Sparkline,展示形式与系统级一致。真机验证:通用模型decode 37.1 tok/s入库,历史含两模型曲线 |
| v1.8.2 | 2026-06-29 | 新增API清单.md:整理全部38个接口(管理REST/对外OpenAI+Claude网关/WebSocket主题),含序列/方法/路径/鉴权/功能描述/参数描述,按功能分组 |
| v1.8.1 | 2026-06-29 | 文档更新至v1.8.0现状:README/操作手册/上下文交接补充Claude CLI连接说明、日志WS化、性能测试增强、tok-s、SearXNG base_url修复;测试checklist追加v1.6.0~v1.8.0验证(阶段十三)与前端通信方式现状;研发方案追加性能/观测/网关增强与前端通信现状章节 |
| v1.8.0 | 2026-06-29 | API调试页:Temperature改场景化下拉+System/Prompt默认示例;前端通信审计并把最后的轮询(组件日志/模型配置页日志)迁移到WebSocket(新增set_log_target动作+logs主题点对点推送+每连接send_lock防并发写),组件状态/总览/运行观测已是WS;/v1/* OpenAI+Claude对外REST保持不变并验证200 |
| v1.7.0 | 2026-06-29 | 性能测试增强:①实时进度——每个请求完成即通过WS perf_progress广播completed/total/failed,前端显示进度条+x/y百分比;②健壮性与排障——失败请求在报告中记录完整请求包(method/url/headers/body)与响应包(状态码+响应体,截断2KB),前端失败行可展开查看;网络层错误(超时/拒绝)也记录请求与异常类型 |
| v1.6.3 | 2026-06-29 | 修复Claude CLI(/v1/messages)报错400 system role:网关转发前清洗Anthropic请求体,把混入messages数组的system角色消息合并进顶层system字段(messages仅留user/assistant),count_tokens同样处理;非流式与流式(SSE)均真机验证通过 |
| v1.6.2 | 2026-06-29 | 修复SearXNG生成链接硬指向localhost的问题:移除compose里固定的SEARXNG_BASE_URL=localhost,改为searxng-settings.yml的server.base_url=false让其按请求Host自动推导对外地址(opensearch.xml/偏好设置等链接随访问IP正确生成,支持192.168.199.4/tailscale等多IP) |
| v1.6.1 | 2026-06-29 | 性能测试历史记录支持删除:新增 DELETE /api/perf/reports/{id}(单条,含路径穿越防护)与 DELETE /api/perf/reports(清空全部);前端历史表每行加删除按钮+表头清空全部按钮,均带二次确认 |
| v1.6.0 | 2026-06-29 | 性能测试401修复(perf转发补vllm_api_key Bearer);运行观测模型级tok/s实现(从vllm /metrics的prompt/generation_tokens_total计数器差分算prefill/decode tok/s,runtime-stats返回+前端实时sparkline+运行表新增tok-s与累计tokens行);高级设置SearXNG检索URL占位符改为window.location.hostname+searxng_port真实值 |
| v1.5.1 | 2026-06-29 | 文档全面更新至v1.5.0现状:README/操作手册重写(移除ollama/GGUF走vllm/单模型约束/手动扫描/对外API网关),测试checklist补充v1.1.1~v1.5.0验证汇总+GPU审计,研发方案追加阶段八~十二架构现状综述,技术方案加实现现状修订说明,上下文交接补v1.4.1/v1.5.0 |
| v1.5.0 | 2026-06-29 | 模型扫描改为手动+持久化缓存:新增web/model_cache(进程内缓存+磁盘model_scan_result.json,按mtime自动重载),/api/models/rescan手动触发,前端重新扫描按钮+上次扫描时间,启动只load不scan(首run才bootstrap),所有读路径走缓存(overview/models/api-directory),CLI model_check统一用save_scan_result;GPU采集优化:无WS客户端连接时跳过重型页面快照广播(docker stats等)降低空闲消耗 |
| v1.4.1 | 2026-06-28 | scan_models多维度文件校验:用index.json权重清单(weight_map)做权威分片完整性核对+total_size大小/截断核对+config关键字段+空文件检测,结果按目录签名缓存;新增可选SHA256深度校验(cli model_check --verify-hash,读取哈希清单/sidecar) |
| v1.4.0 | 2026-06-28 | 新增本地模型文件完整性校验(缺分片/无权重/GGUF不兼容,前端高亮+加载422门禁+CLI标红);新增OpenAI+Claude统一对外API网关(端口8000,/v1/*按model路由,Bearer+x-api-key双鉴权,支持stream);API发布页与健康检测改为单一spec来源 |
| v1.3.0 | 2026-06-28 | 组件状态显示绑定IP+内存MB+SearXNG可启停;修复API健康检测名称错配与Claude鉴权提示(Bearer);组件日志改名+端口标注;禁用SearXNG brave引擎;运行观测量化显示dtype;API调试增加Embedding测试;高频接口(overview/api_directory/runtime_stats)改WebSocket推送 |
| v1.2.1 | 2026-06-28 | 修复API调试转发401(补Authorization头)+组件状态内存MB标注+SearXNG启停重启+API调试页重设计(常用参数前移/支持接口表/system字段说明) |
| v1.2.0 | 2026-06-28 | **按用户5项需求迭代+全代码细查**：①**单模型加载限制**：通用/嵌入各只能同时加载1个(固定容器名gb10-vllm-general/embedding)，`models_router.load`在加载前用`docker inspect`读固定容器槽真实`--served-model-name`(新增`web.state.current_model_in_container`)，若该槽已是别的模型则返回409并提示"请先卸载「X」"，重新加载同名模型仍允许(幂等重启/同步key)。真机验证：嵌入槽已加载2B时再加载8B返回409+中文提示；重载同名返回200 Recreated。②**API健康主动检测**：新增`GET /api/api-directory/health-check`真实探测Web后端/通用模型/嵌入模型(/v1/models带Bearer)/SearXNG(真实JSON搜索)，401/403识别为"密钥不匹配"并提示重载模型同步key；实时总览→API发布页加"检测API健康状态"按钮+逐行健康列+总体指示。真机验证：检测出嵌入容器烧录的是旧vllm_api_key(sk-4b20…而config已是sk-313e…)→重载该模型后探测转正常。③**版本发布脚本**`tools/release.sh vX.Y.Z "说明"`：版本号格式校验+提交者兜底+tag重名保护+自动追加Task_Tracking.md版本行+打tag。④**CLI健壮性修复**：`python3 -m cli.main start`即使不在venv里也能起web后端了(此前必报"No module named uvicorn")——`process_manager`新增`_web_python_executable()`优先用`.venv/bin/python`跑uvicorn。真机用裸`python3 -m cli.main restart`验证web后端正常启动。⑤**组件全面体检(重点SearXNG)**：SearXNG容器Up、`/api/searxng/status`=running、真实搜索`nvidia`返回35条结果、内存118MB；Web/前端/嵌入模型(8002端口/4GB)均running。另：git清理(运行态pid/log不再入库)。 |
| v1.1.4 | 2026-06-28 | **全面API接口审查(27个/api/*)**：发现并修复2个bug+1项性能优化。bug① `/api/settings` GET/PUT schema均缺失`frontend_port`字段，导致高级设置页无法查看/修改前端端口——补全SettingsOut/SettingsUpdateRequest+前端表单。性能优化：`/api/components`原对3个固定容器各自调用一次`docker inspect`(3次子进程spawn)，改为单次批量inspect；经计时验证该接口剩余~2s耗时的真实瓶颈是`docker stats --no-stream`本身在本机的固有开销(已用独立计时区分两者，非可继续优化的代码路径)。其余overview/models/logs/searxng/perf/metrics/runtime-stats/api-directory/auth/health等接口逐一真机curl验证，均200且响应时间正常，未发现其他bug。 |
| v1.1.3 | 2026-06-28 | 新增`frontend/public/favicon.ico`（无PIL依赖，手写ICO字节构造NVIDIA绿色GPU芯片图标），index.html新增icon link引用，已构建并真机验证`GET /favicon.ico`返回200 image/x-icon。 |
| v1.1.2 | 2026-06-28 | **修复2个真实bug**：①`param_advisor.py`的`quantization`参数默认值改为`None`(交由vllm从config.json的quantization_config自动检测)，而非照搬model_scanner猜测的展示标签(如目录名"NVFP4")——vllm 0.21真实QUANTIZATION_METHODS注册表中并无"NVFP4"，照搬必然导致启动失败；QUANTIZATION_OPTIONS同步换成真实29个注册值。②`config.py`的`load_config()`补上"文件存在但缺少新增字段"场景的检测与立即落盘，修复`vllm_api_key`(v1.1.0新增)每次调用都重新生成随机值、导致已加载模型Bearer鉴权必然401 Unauthorized的bug(与此前secret_key同类问题同根因，第二次复现)。真机验证：Gemma-4-26B-A4B-NVFP4(NVFP4量化)不传--quantization正确自动检测为modelopt并加载成功，Bearer鉴权推理请求返回正常结果。另发现`Qwen3-VL-30B-A3B-Thinking-NVFP4`缺失`model-00002-of-00004.safetensors`分片文件(数据完整性问题，需用户重新下载，与本次修复无关)。 |
| v1.1.1 | 2026-06-28 | **联机调试修复4个真实bug**：①`/api/overview`等接口随机超时/"加载总览数据失败"——根因是`model_scanner.scan_models()`对每个GGUF文件用`gguf.GGUFReader`做兼容性检测，每次API请求都重新打开/扫描全部5个本地GGUF文件(含60GB+56GB超大文件)，单次耗时30+秒；修复为按(路径,mtime,size)缓存检测结果，二次调用从30s降到0.03s，并在`web/main.py`启动事件中`await`预热一次(放在`Application startup complete`之前完成)，避免与首个真实请求竞争重复扫描。②组件日志`/api/logs/{component}`无法按容器名(如`gb10-vllm-embedding`)或固定别名(`web`/`frontend`)查询，只认已加载模型名——新增`docker logs`直查能力+`web`/`frontend`别名(读取`data/logs/*.log`)，不再依赖registry状态。③`cli start/restart`启动顺序错误：Web后端先于模型容器启动，导致`reconcile_from_disk()`对账时模型容器尚未创建，组件状态表把已加载的嵌入模型显示成空的"未加载"槕位——调整为先启动SearXNG+模型容器、最后才启动Web后端。④`ComponentLogsView.vue`/`logs_router.py`前后端标识符不匹配导致"时而为空时而报错"，统一为容器名+`web`/`frontend`/`searxng`别名体系。全部在真机重启验证：`/api/overview`首次~19s(预热)、二次2.1s；`/api/models`0.05s；组件状态正确显示已加载嵌入模型及端口/内存；日志查询`gb10-vllm-embedding`/`web`均返回正确内容。 |
| v0.1.0 | 2026-06-27 | 阶段一：core 核心库（环境检测/模型扫描/参数推断/docker封装）+ CLI骨架，已在服务器真实环境验证（环境检测4项OK，33个模型扫描全部正确识别） |
| v0.2.0 | 2026-06-27 | 阶段二：FastAPI Web后端（鉴权/overview/模型/组件/env/logs/debug/settings路由 + WebSocket Hub + 指标采集），已在真实环境验证登录鉴权、overview聚合、模型列表、危险操作确认门禁。修复 `core.config.load_config` 在配置文件不存在时每次调用都重新生成随机密钥/密码的bug（导致JWT签发与校验密钥不一致、登录必然失败），改为首次加载即落盘。 |
| v0.3.0 | 2026-06-27 | 阶段三：Vue3 前端九页面（登录/总览/通用模型配置/嵌入模型配置/运行观测/API调试/组件日志/性能测试/高级设置），表单由后端 param_advisor 返回数据驱动而非硬编码。已在服务器 `npm install && npm run build` 类型检查通过，前后端联调（vite preview + uvicorn 同时运行）均返回200。 |
| v0.4.0 | 2026-06-27 | 阶段四：自动部署脚本(deploy/bootstrap.sh) + docker compose真实模板 + SearXNG集成。SearXNG容器真实拉起，`/search?format=json` 返回真实搜索结果；后端 `/api/searxng/*` 接口联调通过。 |
| v0.4.1 | 2026-06-27 | **关键bug修复**：`core/docker_helper.py` 渲染的vllm compose command缺少可执行文件(`vllm serve`)和`--model`路径参数，且flag/value未拆分为独立token，导致模型**必然无法加载**。修复后在真实GPU上完整跑通：通过Web API触发加载7B模型(WhiteRabbitNeo-2.5-Qwen-2.5-Coder-7B) → vllm容器真实加载14GB权重 → OpenAI兼容API返回真实推理结果 → 卸载容器，全链路闭环验证通过。 |
| v0.5.0 | 2026-06-27 | 测试checklist.md + 操作手册.md + Task_Tracking.md，沉淀已验证测试项、4个已修复bug、待办清单。 |
| v0.6.0 | 2026-06-27 | **阶段五**：ollama容器化（用户明确要求容器方案，非原生安装）——Modelfile + `docker exec ollama create` 流程；性能测试模块（轻量级自定义并发吞吐测试，未对接MLPerf等标准benchmark）；运行观测指标历史落盘+查询；API调试真实转发(OpenAI/Claude格式)。真实GPU验证：ollama加载15GB gguf模型推理成功(15 t/s)、4并发性能测试统计正确、调试转发openai/claude两种格式均验证通过。修复"gguf模型默认走vllm引擎导致崩溃"的引擎路由bug。 |
| v0.7.0 | 2026-06-27 | **阶段六：全面查缺补漏**。修复严重bug：vllm 0.21+已移除`--task`参数（改用`--convert`+`--runner`），导致embedding模型加载必然崩溃——修复后真实验证embedding模型生成向量成功。修复通用/嵌入模型同时加载默认共用8001端口导致冲突的问题。补上SearXNG组件日志查看。验证：高级设置PUT真实落盘生效、WebSocket指标推送真实工作(python客户端验证)。确认并记录架构限制：ollama容器方案不支持多分片GGUF模型自动合并加载（需用户预合并），已用真实9分片31B模型复现报错。至此，Project_Task.md所有功能需求均已实现并在真实ARM64+GPU环境验证或明确记录限制，无遗留占位/mock代码路径。 |
| v0.8.0 | 2026-06-27 | 密钥/密码改为全程明文显示；Web九页面布局全面重新设计（响应式grid、统一状态色、危险操作视觉区分、表单分组折叠）。 |
| v0.9.0 | 2026-06-27 | **阶段七：全面核对需求文档后修复5个缺口**——CLI start/stop/restart/clean真实驱动(新增core/process_manager.py)；环境异常修复增加CLI提示与Web一键修复并存；新增"API发布"看板(/api/api-directory)；组件内存使用+运行观测KV Cache/启动参数真实数据(新增/api/models/{name}/runtime-stats)；SearXNG代理配置持久化。验证过程中额外发现并修复3个bug：registry纯内存导致CLI启动的模型Web看不到（新增web.state.reconcile_from_disk()按真实容器`--served-model-name`对账）、过期compose文件堆积导致模型名称与运行容器张冠李戴、内存单位解析永远失败(`_parse_mem_usage`字典遍历顺序bug)、vllm KV Cache指标名猜测错误。全部在真机ARM64+GPU环境逐项验证通过。遗留发现：ollama容器重建后`ollama list`为空但数据卷有数据，疑似持久化机制问题，已记录待查。 |
| v1.1.0 | 2026-06-28 | **阶段八：GGUF引擎统一改造+页面调整**——按用户要求完全移除ollama引擎，GGUF模型统一改走vllm加载(`render_compose`新增gguf文件路径定位+强制float16+不兼容检测)；新增`model_scanner`对GGUF量化类型与vllm支持集合的比对(发现`GPTOSS-120B-MXFP4`不兼容)；新增`vllm_api_key`(sk-xxxx)全局鉴权并接入所有vllm容器；修复API发布页IP硬编码127.0.0.1的bug(改为真实局域网IP检测)，拆分展示OpenAI/Claude两种接口；总览页组件状态改为始终展示5个固定关键组件而非空列表；max_model_len改为12档下拉(通用64k/嵌入4k默认)；tool_call_parser随enable_auto_tool_choice勾选联动启用，选项来自真实`vllm serve --help`读取的39个parser；API调试页展示该名称列表。全部真机验证通过(GGUF模型端到端推理成功)。 |
| v1.0.2 | 2026-06-28 | 新增`tools/gguf_merge_shards.py`：服务器上确认无官方`llama-gguf-split`二进制，用Python `gguf`包的`GGUFReader`/`GGUFWriter`自实现多分片GGUF合并工具。已用真实9分片31B模型(`Gemma-4-31B-JANG_4M-CRACK-GGUF`)完整验证：合并出30.38GiB单文件→`ollama create`成功→真实推理请求返回正常结果。原"多分片GGUF无法加载"的架构限制已解除，仅需用户手动跑一次合并脚本。 |
| v1.0.0 | 2026-06-27 | **CLI 一键整合**：`python -m cli.main start` 现在统一拉起 Web后端 + 前端(自动`npm install && npm run build`，无dist时才构建+`vite preview`对外服务) + SearXNG + ollama共享容器 + 已配置模型，不再需要分别手动执行 docker compose/uvicorn/npm 命令；`stop`/`clean` 同样统一处理全部组件。新增`AppConfig.frontend_port`配置项。过程中发现并修复一个真实bug：`npx`是包装进程会fork出真正的node子进程，SIGTERM只杀包装层、子进程仍在跑且继续对外响应——改为对整个进程组(`os.killpg`)发信号，同时修复了web后端理论上的同类风险。已在真机验证：start后前端/后端均真实可访问(200)，stop后端口彻底释放(连接拒绝)。已更新操作手册.md。 |

## 工作流程进度

| 步骤 | 角色 | 任务 | 状态 |
| --- | --- | --- | --- |
| 1 | 产品经理 | 技术方案.md（ABC三方案对比） | ✅ 完成 |
| 2 | 项目经理/用户 | 确认技术方案（方案A） | ✅ 完成 |
| 3 | 研发经理 | 阶段一：core核心库 + CLI骨架 + 研发方案.md | ✅ 完成，已部署服务器验证 |
| 4 | （新增）| 远程登录工具 tools/remote.py | ✅ 完成并验证 |
| 5 | 研发经理 | 阶段二：FastAPI Web后端（鉴权/REST/WebSocket Hub） | ✅ 完成，已真实验证（v0.2.0） |
| 6 | 研发经理 | 阶段三：Vue3 前端九页面 | ✅ 完成，服务器构建通过、联调通过（v0.3.0） |
| 7 | 研发经理 | 自动部署脚本 + docker compose 真实模板 + SearXNG集成 | ✅ 完成，SearXNG真实拉起验证（v0.4.0） |
| 7a | 研发经理 | 关键bug修复：vllm compose command渲染 | ✅ 完成，真实GPU全链路闭环验证（v0.4.1） |
| 8 | 测试经理 | 测试checklist.md + 执行测试 | ✅ 完成并持续更新，详见测试checklist.md（v0.5.0起） |
| 9 | 产品经理 | 操作手册.md | ✅ 完成（v0.5.0） |
| 10 | 研发经理 | 阶段五：ollama容器化(Modelfile方案) + 性能测试 + 运行观测历史 + API调试转发 | ✅ 完成，真实验证（v0.6.0） |
| 11 | 测试经理+研发经理 | 阶段六：全面查缺补漏，修复embedding加载/端口冲突/searxng日志三个bug | ✅ 完成（v0.7.0） |
| 12 | 研发经理 | 密钥/密码明文显示（按用户要求取消脱敏） | ✅ 完成（本轮） |
| 13 | 研发经理 | Web九页面布局重新设计（不严格按文档排版，按内容科学排版） | ✅ 完成（本轮） |

## 待办事项（已知架构限制，非代码bug，暂不处理）
- 性能测试模块未对接MLPerf/LLMPerf等国际标准benchmark
- 高并发/多用户登录场景未专项测试（内部工具，用户量低，非重点）
- `GPTOSS-120B-Uncensored-HauhauCS-Aggressive-MXFP4.gguf`：vllm 0.21的GGUF加载器不支持MXFP4量化类型，无法加载（已在model_scanner/docker_helper/models_router三层校验拒绝，给出明确错误提示）

## 工具脚本（非Project_Task.md原定范围，按需追加）
- `tools/gguf_merge_shards.py`：多分片GGUF合并为单文件，用法见 测试checklist.md「多分片GGUF合并工具」一节。
